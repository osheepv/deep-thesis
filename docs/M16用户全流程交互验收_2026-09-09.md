# M16 用户全流程交互验收（2026-09-09）

## 2026-09-10 补验与更正

9 月 9 日的全量测试和 CI 通过，但当时浏览器使用的后端没有重启加载修复，因此当日记录中的“上传后重试成功”不能视为修复后浏览器验收证据。

9 月 10 日重启预览后端，使用原隔离数据目录与任务 `TASK-BE7913EF5B82`，通过 Playwright 打开任务并点击“执行当前环节（环3 文献调研）”。页面显示 1 篇候选文献和“确认进入下一环”，验证原上传文件在进程重启后仍能读取，空池重试确实恢复到待审批状态。浏览器证据保存在验收输出目录 `.playwright-cli/page-2026-09-09T23-58-46-626Z.yml` 附近的快照；UTC 时间对应北京时间 9 月 10 日。

本次另发现单条候选已自动纳入，但即时执行响应没有 `included_indexes`，页面因而错误显示“已保存：纳入 0 条”。现已补齐该字段，并在空池重试回归中断言 `curated=true`、`included_indexes=[0]`。针对性测试 10 项通过。该字段修复通过自动化契约检查，尚未再次做新的即时执行浏览器复验。

知识库同时显示原上传文件及派生题录，后续应检查展示去重与文件关联。完整十环浏览器验收仍未完成；本阶段仅覆盖前述环1至环3及续作恢复路径。

## 目标

### 2026-09-10 真实环6与运行中刷新

- 使用本机既有DeepSeek配置，仅对验收进程启用真实模型并关闭Mock回退，沿用隔离测试任务和测试资料。前五环为之前的离线流程，不属于真实模型十环验收。
- 通过作业面板将单次Token预算设为80,000，点击环6执行。作业 `JOB-450F47D62F3C4AA38340` 首次尝试成功，消耗10,320 Token，生成11,448字初稿。
- 运行期间刷新浏览器，后端作业持续完成，未重新发起生成。但刷新后的界面未自动接续轮询，停在旧运行状态；再次手动刷新才能看到待审批。这是待修复的前端续作问题。
- 本次修复历史完成卡片的过时阶段提示，改为“本环产物已确认”。刷新后浏览器验证该文案正确；28项UI相关测试及JavaScript语法检查通过。
- 人工确认环6后进入环7，并以同样80,000 Token上限开始真实润色。该测试材料不可作为真实研究成果提交。
- 环7作业 `JOB-F60B861C4F2A46F29998` 首次尝试成功，消耗16,342 Token，润色稿11,450字。页面未刷新时正常更新待审批摘要和确认入口；上述字数不是论断真实性评测。
- 人工确认环7后，把预算调整为5,000 Token执行环8。返回“文献检索禁用（THESIS_LIT_ENABLED=false），1条全部待人工复核”，页面显示回到环6修订及回到环3整理文献的按钮，没有下一环确认入口。此结果只验证检索关闭时的阻断，不证明已联网核验或检测出虚构资料。
- 本轮真实模型环6与环7共消耗26,662 Token；环8未消耗模型Token。任务保留在环8失败可恢复状态，环9及环10未执行。

### 2026-09-10 静态缓存与环6阻断补验

- 统一启动器新增 `serve_preview_ui.py`，仅监听本机；静态响应禁止存储缓存，旧 `If-Modified-Since` 请求也返回200与完整脚本。构建清单包含该入口。
- `tests/test_preview_bundle.py` 的真实进程测试同时检查HTML/脚本缓存头、条件请求、API存活、任务保存和退出回收，3项通过。
- 浏览器打开新版服务后大纲正常显示，经人工确认进入环6；点击执行后出现“真实模型不可用，降级模板稿禁止进入作者审批”，没有下一环确认按钮，当前环执行按钮恢复可用。
- 本次验证的是Mock模式下的失败阻断，不是环6正文质量或环7至环10成功路径。
- 另记录已有交互问题：阶段推进后，历史完成卡片中的“继续环5”文字没有随当前环更新；当前操作按钮和恢复摘要正确显示环6。后续修复历史提示文案。

### 2026-09-10 环3至环5续作补验

- 上一修复提交 `bdcc84e` 的 GitHub CI 已通过。
- 原任务重新打开后，环3显示“已保存：纳入 1 条”，点击确认进入环4。
- 环4执行后显示评审结论，人工确认后进入环5；环5生成大纲并停在待审批状态。
- 刷新后发现大纲只剩待确认提示，正文没有恢复。原因是进度接口和历史恢复界面只允许环1、3、8携带及展示待审产物。
- 本次将环2、4、5加入恢复范围，复用既有产物组件。重启后端并清除浏览器旧脚本缓存后，页面重新显示 5 个章节和 13 个分节合同，保留人工确认按钮，未重新生成大纲。
- 新增回归覆盖开题、综述和大纲在确认前重新读取进度时提供完整待审产物；离开待审状态后不继续返回该载荷。相关测试 `test_full_flow_hardening.py` 与 `test_ui_workbench.py` 共39项通过，JavaScript语法检查通过。
- 待处理：发布升级时的静态资源缓存更新；知识库原件与派生题录关联展示；环6至环10浏览器验收。本次仍使用离线测试资料及Mock模式，未运行真实模型。

用普通用户视角检查预览版的连续写作路径，重点验证“今天停下、下次继续”以及文献不足时的可恢复交互。验收使用本地 Mock/离线模式，不把测试结果当成真实论文质量结论。

## 环境与步骤

- 通过 `scripts/run_preview.py` 启动隔离数据目录中的 API 和前端。
- 通过 Playwright CLI 操作真实浏览器，验证可访问名称、表单提交、按钮状态和刷新后的恢复界面。
- 依次执行：新建论文 → 选择研究候选 → 确认环1 → 确认环2 → 刷新页面 → 执行环3。
- 在环3没有在线检索结果时上传一份本地测试文献，登记后重新执行环3。

## 验收结果

- 新建论文表单可提交，任务标题、领域和学位级别正确进入工作区。
- 环1候选选择和环1/环2审批门正常工作，审批前后的按钮状态符合当前阶段。
- 刷新页面后显示“继续上次论文”横幅，并准确恢复到环3待处理状态。
- 环3在没有可用文献时 fail-closed，明确显示阻断原因，不允许直接进入写作。
- 上传本地文献后，离线检索模式会读取知识库中的新材料；环3不再复用先前的空结果缓存，可重新进入待审批状态。
- 环3失败卡片提供“补充文献后重试”入口，并将焦点带到文献上传控件。
- 文献上传入口改为原生按钮和隐藏文件输入，保留 PDF / Word / 题录提示，支持键盘触发。
- 账户区域使用 Deep Thesis 产品品牌文案，不再显示硬编码的个人昵称和 Pro 角色。

## 修复内容

1. 编排层只有在环3存在非空结果时才复用缓存，空结果会在新材料登记后重新执行。
2. 离线文献模式将已登记的本地文献转换为环3候选池，并保留“未在线核验”的提示。
3. 前端为文献不足提供直接补救动作，并收紧已确认阶段的候选按钮状态。

## 回归

- `tests/test_full_flow_hardening.py`：10 passed。
- `tests/test_preview_bundle.py`：3 passed。
- `node --check ui/js/app.js`：通过。
- M16 新增空文献重试回归测试；全量回归基线为 446 项 pytest，学术质量离线评测为 31/31。

## 已知限制

- 本次使用离线/Mock 模式，未覆盖真实 DeepSeek 网络、真实下载文献和生产部署环境。
- Cytoscape 依赖仍会输出 8 条已有的非阻断警告（滚轮灵敏度、文本边框属性和缺失 degree 映射）；未修改第三方库。
- 完整十环真实用户浏览器验收、免安装桌面打包和公网发布仍属于后续发布工作。
