# M12 停机备份与恢复验收

日期：2026-09-08。

## 已交付

- 新增 `application.backup` 和安装入口 `deep-thesis-backup`：create / verify / restore。
- 备份清单登记原路径、文件大小与 SHA-256；覆盖数据库、WAL 和目录内附件。
- 临时目录验证后发布；拒绝已有目标、嵌套目标、链接、已知目录外配置与复制期间变化。
- SQLite 完整性检查在临时副本中执行；恢复前后校验，禁止覆盖既有数据和跨路径恢复。
- 操作指南说明停机声明、外部配置边界、原数据保留和回退流程。

## 验证

本地执行 `pytest tests/test_offline_backup.py tests/test_standalone_worker.py tests/test_docx_persistent_registry.py -q`：21 项通过，20.68 秒。新增 11 项备份测试，并扩展原跨进程测试：API 创建任务、独立 Worker 执行、生成成稿，全部退出后备份，保留原目录并恢复，在新进程读取产物和作业、启动对账为 CONSISTENT、经真实下载路由取得成稿。

负向测试包括备份内容修改/丢失/额外文件、清单路径穿越、数据库损坏、配置目录外引用、目标覆盖、跨路径恢复和复制期间写入。WAL 提交恢复也已验证。

离线学术质量规则：31/31 通过，无模型调用。该结果不代表真实模型论文质量评测。

## 边界与后续

这一交付是 M12 的停机备份与恢复部分。停机依赖操作人员确认，非在线一致性快照；检查文件摘要不能保证有写进程运行时的跨库一致性。工具不迁移现有用户数据，也不自动停服务。旧分散存储、跨路径迁移、自动备份、发布环境强杀断网和压力验收尚未完成。

运行步骤见[数据备份与恢复指南](数据备份与恢复指南.md)。本地测试使用隔离数据，未移动或删除现有论文。
