# M15 首次安装与发行包验收

## 环境与安装

使用 M14 提交 `bcb5dfb` 生成的 ZIP，在独立目录解压。新建 Python 3.13.14 / Windows x64 虚拟环境，`include-system-site-packages=false`，没有复用项目测试环境的 site-packages。Python 基础解释器来自本机已有安装，因此这不是全新 Windows 系统验收。

按指南执行 `python -m pip install --index-url https://pypi.org/simple ./backend`，从官方 PyPI 安装运行依赖并成功构建/安装 `deep-thesis 0.1.0`。安装日志保存在本机隔离验收目录，未修改项目运行环境、用户配置或原有论文。

## 发行包验证

新增 `scripts/verify_installed_runtime.py --output-dir ABSOLUTE_NEW_DIRECTORY`，先执行 `pip check`，再用 `python -I` 在源码目录外启动两个进程。通过发行包元数据和模块路径检查，确认导入来自新虚拟环境的 site-packages，而非工作区源码。

本地结果：PASS。

- 依赖一致性检查通过。
- 10 份包内提示词可加载。
- 创建任务、环1离线候选和 revision=2 的未提交草稿。
- 从包内模板生成真实成稿并登记，保存本地文献。
- 第二个进程启动后读取环产物和待审批阶段、草稿内容与版本、文献文件；成稿通过下载路由读取成功。
- 两个进程启动对账均为 CONSISTENT。

自检显式禁用真实模型调用与模型仓库下载；测试模式只在该进程生效。结果记录为 `report.json`，依赖和每阶段日志存于同一个独立输出目录。输出目录要求尚不存在，避免覆盖已有验收或用户数据。

## 解压包启动验收

使用上述新环境运行解压包原有 `scripts/run_preview.py`，将测试数据放在解压包之外。真实 HTTP 检查：首页、主脚本、API 健康与对账均通过，创建任务成功。通过定时进程内 Ctrl+C 结束验证服务。

本次未发现需要调整的运行依赖或包资源。`tests/test_preview_bundle.py` 相关回归 3 项通过，23.54 秒；构建器现在把安装自检脚本一同收入预览包。CI 增加安装发行包并在源码之外运行自检的步骤，未来包内容缺失会使构建失败。

## 边界与下一步

这不证明真实 DeepSeek 写作质量、在线文献检索、嵌入模型下载、视觉交互或全新 Windows 系统兼容性。下一步可验证干净系统上的安装体验、提供安装引导，并开展真实用户试用。公网发布、免安装桌面版、签名和自动升级仍待完成。
